/**
 * This test creates a Staged Prop, then has different wallets fund it. 
 * When funded, proposer can call initialize and create the the Metadao Proposal
 * After the proposal gets traded, it can be finialized and tokens plus rewards returned to LPs.
 */

async function main() {
    
}

// Proposer Functions

// LP Functions


main().then(() => {
    console.log("Test completed");
}).catch((error) => {
    console.error("Test failed:", error);
});